--- /tmp/kvm_host.h.orig
+++ /usr/src/linux-6.18.7/arch/x86/include/asm/kvm_host.h
@@ -1487,7 +1487,8 @@
 	u32 max_vcpu_ids;
 
 	bool disable_nx_huge_pages;
 	bool rdtsc_exiting;
+	bool hlt_user_exit;
 
 	/*
 	 * Memory caches used to allocate shadow pages when performing eager
