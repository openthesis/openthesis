--- /tmp/x86.c.orig	2026-06-06 00:00:00.000000000 +0000
+++ /usr/src/linux-6.18.7/arch/x86/kvm/x86.c	2026-06-06 00:00:00.000000000 +0000
@@ -5019,6 +5019,9 @@
 	case KVM_CAP_RDTSC_EXITING:
 		r = 1;
 		break;
+	case KVM_CAP_HLT_USER_EXIT:
+		r = 1;
+		break;
 	default:
 		break;
 	}
@@ -6768,6 +6771,13 @@
 		kvm->arch.rdtsc_exiting = true;
 		r = 0;
 		break;
+	case KVM_CAP_HLT_USER_EXIT:
+		r = -EINVAL;
+		if (cap->flags || cap->args[0])
+			break;
+		kvm->arch.hlt_user_exit = true;
+		r = 0;
+		break;
 	case KVM_CAP_SPLIT_IRQCHIP: {
 		mutex_lock(&kvm->lock);
 		r = -EINVAL;
@@ -11699,6 +11709,12 @@
 int kvm_emulate_halt(struct kvm_vcpu *vcpu)
 {
 	int ret = kvm_skip_emulated_instruction(vcpu);
+
+	if (vcpu->kvm->arch.hlt_user_exit) {
+		vcpu->run->exit_reason = KVM_EXIT_HLT;
+		return 0;
+	}
+
 	/*
 	 * TODO: we might be squashing a GUESTDBG_SINGLESTEP-triggered
 	 * KVM_EXIT_DEBUG here.
